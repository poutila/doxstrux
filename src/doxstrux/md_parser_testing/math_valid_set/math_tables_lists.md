---
title: Math Tables & Lists
md_flavor: math
---

# Lists
1. Sum: $S=\sum_i x_i$
2. Limit: $\lim_{h\to 0}\frac{f(x+h)-f(x)}{h}$
- Gaussian: $\mathcal{N}(\mu,\sigma^2)$
- Display in bullet:
  $$
  \mathrm{KL}(p\|q)=\int p(x)\log \frac{p(x)}{q(x)}\,dx
  $$

# Table
| Symbol | Meaning | Example |
|---|---|---|
| $\theta$ | parameter | $\theta\in\mathbb{R}^d$ |
| $\phi$ | features | $\phi(x)=Wx+b$ |
| $\mathbb{E}$ | expectation | $\mathbb{E}[X]=\sum_x x\,p(x)$ |

Note on currency vs math: price is \$5; keep $x+y$ as math.
