# Inline code with special characters

`<html>` tags in code

`**not bold**` in code

`*not italic*` in code

`~~not struck~~` in code

`code with \`backticks\`` escaped

``code with `single` backtick inside``

```triple backtick``` inline?

`C:\\path\\to\\file` backslashes
