# AUTO-GENERATED — DO NOT EDIT BY HAND
# Generated from: AI_TASK_LIST_SPEC (v2.0.0)
# Regenerate using: speksi re-generate

name: Drift Detection (ai_task_list)

on:
  push:
    paths:
      - '**/*_SPEC.md'
      - '**/mapping/*.yaml'
      - '**/stubs/*.py'
  pull_request:
    paths:
      - '**/*_SPEC.md'
      - '**/mapping/*.yaml'
      - '**/stubs/*.py'
  schedule:
    # Run daily at midnight UTC
    - cron: '0 0 * * *'

jobs:
  drift-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e .
          pip install pyyaml

      - name: Run drift detection
        run: |
          python -m ai_task_list.drift.run_all ${{ github.workspace }}/generated
        continue-on-error: false

      - name: Report drift
        if: failure()
        run: |
          echo "::error::Drift detected between specification and generated code"
          echo "Run 'speksi re-generate' to synchronize"
